import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  // กำหนดค่าของธีมที่ต้องการ
  palette: {
    primary: {
      main: '#1976d2',
    },
    secondary: {
      main: '#dc004e',
    },
  },
});

export default theme;
